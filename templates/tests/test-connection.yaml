apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "project-m.fullname" . }}-test-connection"
  labels:
    {{- include "project-m.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "project-m.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
