apiVersion: v1
kind: Service
metadata:
  name: {{ include "project-m.fullname" . }}
  labels:
    {{- include "project-m.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "project-m.selectorLabels" . | nindent 4 }}
